program beta {
  type int = Int<31>;

  type Input = struct {int alice, int bob};
  type MyStruct = struct {int alice, int bob};

  function MyStruct output(Input input, int derp) {
    output.alice = input.alice - input.bob;
    output.bob = input.bob - derp;
  }
}
